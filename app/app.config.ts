export default defineAppConfig({
  ui: {
    colors: {
      primary: 'green', // Vert drapeau malien
      secondary: 'yellow', // Jaune drapeau malien
      success: 'green', // Messages de succès
      info: 'blue', // Informations
      warning: 'yellow', // Avertissements
      error: 'red', // Rouge drapeau malien pour les erreurs
      neutral: 'slate' // Texte, bordures, arrière-plans
    }
  }
})

